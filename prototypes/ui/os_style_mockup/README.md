# OS Style Mockup

This mockup is the first visual direction for FinSkillOS v2.1.

Use it as a structural reference for:

- Control Room status density
- OS-style navigation
- Risk and goal summary hierarchy
- Dark operational UI tone

The production UI lives in `finskillos/ui/`.
