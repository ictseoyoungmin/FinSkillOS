import { PlaceholderPage } from "../PlaceholderPage";

export function SymbolLabPage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Symbol Lab"
      testId="symbol-lab-page"
      message={
        "Ticker-specific state and position context. UI implementation " +
        "deferred to a follow-up slice; Streamlit Symbol Lab remains " +
        "available for debug use."
      }
    />
  );
}
