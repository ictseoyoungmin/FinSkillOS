import { PlaceholderPage } from "../PlaceholderPage";

export function MissionControlPage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Mission Control"
      testId="mission-control-page"
      message={
        "Goal tracker, milestones, and early-stop state. The Control Room " +
        "cockpit already surfaces the mission progress card; the dedicated " +
        "Mission Control page lands in a follow-up slice."
      }
    />
  );
}
