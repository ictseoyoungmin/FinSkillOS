import { PlaceholderPage } from "../PlaceholderPage";

export function MarketKernelPage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Market Kernel"
      testId="market-kernel-page"
      message={
        "Chart terminal and indicator context. The Slice-13.6 frontend " +
        "shell renders this route as a placeholder; the React Market " +
        "Kernel will land in a follow-up slice. The Streamlit page " +
        "remains available for debug use."
      }
    />
  );
}
