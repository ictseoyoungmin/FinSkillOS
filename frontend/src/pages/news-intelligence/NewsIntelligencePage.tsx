import { PlaceholderPage } from "../PlaceholderPage";

export function NewsIntelligencePage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="News Intel"
      testId="news-intelligence-page"
      message={
        "Holdings-relevant news and sentiment overlay. Slice 13.6 reserves " +
        "this route in the OS nav; UI implementation is deferred and the " +
        "Streamlit News Intelligence page continues to serve debug needs."
      }
    />
  );
}
