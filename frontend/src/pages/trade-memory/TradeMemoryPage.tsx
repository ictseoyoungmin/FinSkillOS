import { PlaceholderPage } from "../PlaceholderPage";

export function TradeMemoryPage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Trade Memory"
      testId="trade-memory-page"
      message={
        "Weekly review, mistake-tag frequency, and copyable markdown export. " +
        "UI implementation is deferred; the Streamlit Trade Journal page " +
        "continues to serve reflection workflows."
      }
    />
  );
}
