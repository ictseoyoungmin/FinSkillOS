import { PlaceholderPage } from "../PlaceholderPage";

export function AnalysisWorkspacePage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Analysis Workspace"
      testId="analysis-workspace-page"
      message={
        "Index Lab, ETF tape, and relative-strength panels. The Slice-13.6 " +
        "shell explicitly reserves this route so the React rewrite never " +
        "loses Analysis Workspace from the nav. UI implementation is " +
        "deferred to a follow-up slice."
      }
    />
  );
}
