import { PlaceholderPage } from "../PlaceholderPage";

export function RiskFirewallPage() {
  return (
    <PlaceholderPage
      eyebrow="FinSkillOS · Module"
      title="Risk Firewall"
      testId="risk-firewall-page"
      message={
        "Constraints, limits, and active guards. The Control Room cockpit " +
        "already surfaces a summary; the dedicated React Risk Firewall " +
        "page lands in a follow-up slice."
      }
    />
  );
}
