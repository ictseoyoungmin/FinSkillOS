# v4.1 Product Cockpit visual reference

The HTML visual baseline lives in
`prototypes/ui/enhanced_dashboard_mockup/finskillos_v4_1_product_cockpit_index.html`
inside the monorepo and is **not** served by Vite directly. This
folder is reserved so a follow-up slice can publish a static
side-by-side comparison if needed.

Slice 13.6 leaves it empty intentionally — see
`.devmd/13_6_Frontend_Migration_Shell.md` §Frontend Visual Baseline
for the parity rules (no pixel-perfect requirement, but the OS tray /
ticker strip / Control Room 3-column grid must be preserved).
